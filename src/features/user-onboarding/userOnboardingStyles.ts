import type { SxProps, Theme } from "@mui/material";

export const OnboardingWizardContainerStyles: SxProps<Theme> = {
    padding: "16px 0"
};

export const OnboardingWizardControlsStyles: SxProps<Theme> = {
    display: "flex",
    justifyContent: "space-between",
    marginTop: "20px"
}